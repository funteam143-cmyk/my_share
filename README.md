<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://ai.google.dev/static/site-assets/images/share-ais-513315318.png" />
</div>

# FK Share

Offline peer-to-peer file sharing Android app.

The GitHub Actions workflow builds a debug APK automatically on pushes to `main` or `master`.

## Build

Open **Actions → Build APK**. The generated `app-debug.apk` is uploaded as a workflow artifact.

<!-- Trigger clean project rebuild -->
